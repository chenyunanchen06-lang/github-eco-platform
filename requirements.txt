# 看板 + 查询层 + 智能问数所需依赖 —— 即 Streamlit Community Cloud 需要安装的全部内容。
#
# 刻意不含 pyspark：它是 317 MB 的源码包，云端要现场编译 wheel，又慢又没必要
# （看板只读 demo/demo.duckdb 或 warehouse.duckdb，不碰 Spark）。
# 完整数据管道的依赖见 requirements-pipeline.txt。
duckdb>=1.0
streamlit>=1.30
plotly>=5.20
pandas>=2.0
pyarrow>=14.0
requests>=2.31
